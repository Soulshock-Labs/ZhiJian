# ── 环境变量（含 API Key，绝对不能上传）──
.env
.env.local
.env.*.local

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# ── 虚拟环境 ──
venv/
.venv/
env/
ENV/

# ── pytest / 测试产物 ──
.pytest_cache/
.coverage
htmlcov/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── 生成的 Word 输出（临时文件）──
output_*.docx
filled_*.docx

# ── 本地运行数据 / 统计 ──
app_public_stats.json
feedback_messages.jsonl
weekly_draft_sessions.jsonl
registrations.jsonl
redeem_logs.jsonl
user_accounts.json
user_services.json
partner_webhook_retry.jsonl

# ── 卡密库（真实密钥，勿提交；本地保留一份，部署时随源码上传 Cloud Run）──
redeem_codes.json

# ── Node.js ──
node_modules/
.next/
.nuxt/
dist/
out/

# ── 大型二进制 / 安装包 ──
*.tar.gz
*.zip
*.dmg
*.pkg

# ── Claude Code 工作树 ──
.claude/

# ── Dropbox 冲突副本 / 本地归档 ──
*conflicted copy*
archive/dropbox-conflicts-*/

# ── Google Cloud ──
gcloud-service-key.json
service-account*.json

# ── WeChat Mini Program 本地私有配置 ──
mini-program/project.private.config.json

# ── 本地知识库 / 大数据资料（仅本机 Google Drive，不入 Git）──
knowledge_base/

# -- Kimi / Claude ????? --
PROJECT_CONTEXT.md
.kimi-sync/

